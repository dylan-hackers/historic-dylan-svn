This library implements dynamically scoped bindings. These bindings are
stack-allocated (therefore thread-safe) and accessed using signals and
handlers. No global declarations are needed.

See dynamic-binding.dylan for documentation.
See test/test.dylan for a usage example.

-- Dustin Voss
