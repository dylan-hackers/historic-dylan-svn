            {{if children.size > 0}}
            <ul>
              {{repeat with child in children}}
              <li>
                <a anchor="{{child.id}}" />
                <a href="../{{child.href}}"
                   name="{{child.id}}"
                   title="{{child.shortdesc}}">
                  {{child.title}}
                </a>
{{child.child-recursion !}}
              </li>
              {{end}}
            </ul>
            {{end}}
